# Prettier 忽略范围。
#
# 分两类，理由不同：

# ── 第一类：技术性忽略 ──────────────────────────────────────────────
# 缺了这几条 format:check 必然失败——dist/ 与 pnpm-lock.yaml 都会被判定为
# 格式不合规。见 docs/adr/0003-web-toolchain.md「必要的忽略规则」。
dist
node_modules
pnpm-lock.yaml
coverage

# ── 第二类：限定在前端工作区内 ──────────────────────────────────────
# 前端 CI 只应校验前端自己的代码。以下目录不属于本工作区，且当前没有任何一条
# 是 Prettier 格式化的；若纳入检查，Web CI 会因为别人的文件变红，而「修复」
# 意味着去重排其他成员的文档——既越权，也会产生与本次改动无关的巨大 diff。
#
# 若团队决定做仓库级的文档格式化，那是一个独立的全仓任务，应由 M1 协调后
# 统一执行，并在那一次改动里移除下面这些条目。
apps/api
docs
packages/contracts
.github
