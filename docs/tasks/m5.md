# [M5] 课程、作业、错题与复习业务

负责人：M5 / @Jiege123-CMYK；已设置 GitHub Assignee。评审配对见六人分工表。

## 用户价值

持久化学生学习流程，统一课程、作业、错题和复习计划的业务状态。

## 交付清单

- [ ] 实现课程/章节、作业发布与提交、错题、计划的数据模型与 API
- [ ] 协调数据库迁移顺序并提供一门高数合成示例
- [ ] 基于错误提交/主动确认幂等收录错题，禁止问题一律归错题
- [ ] 校验并保存 M3 导学建议，支持修改计划与站内截止提醒
- [ ] S2 增加间隔复习、同类训练与备考流程，发布学习事件供统计

## 第一阶段

S0 表与 API 样例；S1 作业提交、错题确认、可保存计划。

## 依赖与边界

M1 鉴权；M3 结构化生成；M2 学生 UI；M6 教师入口/统计事件。

负责目录见 docs/team-plan.md；跨模块接口变更须与调用方评审。此卡为 Epic，请拆成 0.5—2 天的小 Issue，每个子 Issue 一个 PR，不一次性交付整个模块。

## 验收证据

重复请求不重复收录；他人作业不可读写；时间/截止边界正确；迁移可从 main 状态升级。

- [ ] 提交具体命令、测试结果或演示证据
- [ ] 解释 AI 生成的关键逻辑，记录 Mock/真实服务区别
- [ ] 非作者评审、PR 合并、集成验收完成

## 进度记录

启动/接口变化/阻塞/评审/验收时更新；工作日结束补：已完成、验证、剩余、阻塞、下一步。

### 2026-09-15 S0 契约草案提交

已完成：
- 建立 packages/contracts/learning/ 下 6 个契约文档（README、courses、homework、mistakes、study-plans、learning-events）
- 写 samples/ 下 7 个样例文件（01-07 + errors.md），覆盖单选提交、简答待批阅、错答自动收录、主动标记去重、M3 导学建议保存、学习事件批量、错误响应
- 适配 M3 在 Issue #17 确认的方案：study-plans.md 改为 M5 service 层按 turnId 调用 M3 取用接口拉取 planSuggestion，前端不转存；校验与取内容合并为一次调用（404 防枚举）
- 发了 3 条新 Issue（#16 请求 M1 鉴权层、#17 请求 M3 导学建议交付方式、#18 请求 M6 教师端字段确认）+ #11 评论催 M1 脚手架合并

验证证据：
- 契约与样例为纯文档，无代码执行；JSON 样例字段命名 camelCase 符合 docs/code-standards.md:48
- S0 阶段只交付契约，不交付实现，无 Mock/真实服务区别
- 待 M2/M3/M6 评审确认后才可视为最终版

剩余工作：
- M1 脚手架合并后落地 apps/api/app/domains/learning/ 的 router/schemas/service/repository 分层（docs/code-standards.md:33）
- 编写 apps/api/migrations/ 下对应表迁移（M5 协调迁移顺序，docs/team-plan.md:27）
- S1 实现：作业提交、错题确认、可保存计划（docs/tasks/m5.md:19）

阻塞/依赖：
- M1 脚手架（PR 待合并，codex/feat/m1-api-bootstrap 分支）—— 阻塞所有 S1 实现
- M3 planSuggestion 字段与取用接口（M3 待开 Issue 扩展 tutoring-response.md）—— 阻塞 source=tutoring 路径
- M6 教师端字段确认（Issue #18 待回复）—— 影响作业发布/课程创建 API 的教师视角字段
- M1 鉴权层角色规则（Issue #16 待回复）—— 影响教师/学生角色鉴权实现

下一步：
- 本 PR 转 Ready 供 M2/M3/M6 评审确认契约
- M1 合并后开 S1 实现子 Issue（每个子 Issue 一个 PR）
