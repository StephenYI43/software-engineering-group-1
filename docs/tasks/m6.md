# [M6] 教师后台、学习统计与成长报告

负责人：M6 / @xk1024；已设置 GitHub Assignee。评审配对见六人分工表。

## 用户价值

支持教师维护课程并查看授权班级学习情况，为学生提供有依据的成长报告。

## 交付清单

- [ ] 独立实现教师端课程上传、解析进度、作业发布与班级概览页面
- [ ] 与 M5/M2/M4 约定学习事件 schema，按 eventId 去重
- [ ] 实现学习时长、答题薄弱点和趋势聚合，注明样本/周期/算法
- [ ] S2 实现知识点雷达图、周报和可解释的预警规则
- [ ] 安排月报、批阅辅助和风格设置增量，补权限与统计边界测试

## 第一阶段

S0 教师原型、事件样例；S1 上传与解析状态、基础班级统计。

## 依赖与边界

M1 教师/班级权限；M3 解析 API；M5 作业/课程及事件；M2 消费学生报告。

负责目录见 docs/team-plan.md；跨模块接口变更须与调用方评审。此卡为 Epic，请拆成 0.5—2 天的小 Issue，每个子 Issue 一个 PR，不一次性交付整个模块。

## 验收证据

教师仅见授权班级；重复事件不重复计时；无数据显示未知；雷达掌握度说明样本来源，不能用主观数字填图。

- [ ] 提交具体命令、测试结果或演示证据
- [ ] 解释 AI 生成的关键逻辑，记录 Mock/真实服务区别
- [ ] 非作者评审、PR 合并、集成验收完成

## 进度记录

### 子任务拆分

GitHub 集成当前缺少仓库 Issues 写权限（创建 Issue 返回
`Resource not accessible by integration`），子任务暂以本表代替在线 Issue，
权限恢复后再补建并回填链接。每个子任务一个 PR，不一次性交付整个模块。

| # | 子任务 | 阶段 | 状态 | 依赖 |
| --- | --- | --- | --- | --- |
| 1 | analytics 学习事件消费契约评审与冻结 | S0 | PR #34；M6 → M2 统计契约 PR #37 | 依赖已满足（#26 已合并），待 M5/M1 按条确认 |
| 2 | 教师端原型：页面结构与事件样例 | S0 | PR #35（`docs/prototypes/`） | 无 |
| 3 | 教师课程上传与解析进度页面 | S1 | 待 M2 工程壳合并后开始 | M2 的 `apps/web` 工程壳（PR #36）、M3 解析 API |
| 4 | 基础班级统计聚合 | S1 | 实现主体完成（PR #38：内核 + 95 例测试） | #26 已合并；仍待 M5 建 `learning_events` 表 |
| 5 | 雷达图、周报与预警规则 | S2 | 未开始 | 子任务 4 |

子任务 3 的顺序被 `apps/web` 工程壳阻塞（ADR-0003：M6 不初始化工程），
故 M6 先做不依赖他人目录的子任务 4，避免空等。

### 2026-09-20

- 已完成：
  - 教师端 S0 原型文档与 11 条合成事件样例（`docs/prototypes/`，PR #35）
  - M6 → M2 统计查询契约草案及 5 个样例（`packages/contracts/analytics/statistics-api.md`，PR #37）
  - analytics 聚合内核：事件模型、`(occurredAt, eventId)` 游标、`eventId` 幂等去重、
    会话推断时长、题型/章节薄弱点、契约响应模型与错误码、端口与内存实现（PR #38）
- 验证（PR #38）：`ruff format --check` / `ruff check` / `mypy` 全绿，
  `pytest` 111 例通过、覆盖率 100%（含并入 main 后的其它域测试）；
  另用一次性脚本把接口响应与契约样例 01 逐字段比对一致。
  未运行数据库迁移与真实 `learning_events` 表读取——上游未落地，不声称 S1 完成。
- 进展：上游 learning-events.md 已随 PR #26 合并进 main，M6 提出的两处修订
  （`eventId` 格式、`isCorrect`/`score` 可空）均落地，契约冻结的前置条件已满足。
- 剩余：子任务 3（等 M2 工程壳）、子任务 5；子任务 1 待成员按条确认后标冻结。
- 阻塞：`apps/web` 工程壳尚在 PR #36（Draft，已请 M6 评审）；
  GitHub Issues 写权限缺失导致子任务无法转为在线 Issue。
- 下一步：完成 PR #36 评审意见以推动工程壳合并；合并后按
  `features/teacher/routes/index.tsx` 的挂载点实现子任务 3。
